# خطة بناء إضافة Cinemana → PotPlayer

إضافة Brave/Chrome (Manifest V3) تلتقط رابط الفيديو المباشر من صفحة حلقة على
`cinemana.shabakaty.com` وتفتحه في PotPlayer على ويندوز عبر بروتوكول `potplayer://`.

---

## 1. الحقائق المؤكدة من الفحص (DevTools)

| العنصر | القيمة |
|---|---|
| صفحة الحلقة | `https://cinemana.shabakaty.com/video/ar/{ID}?showInfo=true` |
| رابط الفيديو الأولي | `https://cdn.shabakaty.com/vascin24-mp4-4k/{VIDEO_ID}_video.mp4?response-content-disposition=...&AWSAccessKeyId=...&Expires=...&Signature=...` |
| الاستجابة | `302 Found` → `Location` يشير إلى نفس المسار على `cndw2.shabakaty.com` (أو `cndw*.shabakaty.com`) |
| نوع الملف | MP4 مباشر (**ليس** m3u8/HLS) |
| الترجمة | **مدمجة داخل الفيديو (burned-in)** — لا يوجد ملف `.vtt`/`.srt` منفصل |
| صلاحية الرابط | موقّع بـ AWS signature ومحدد بـ `Expires=` (timestamp) — الرابط ينتهي بعد فترة |
| متى يتولد الرابط | فقط بعد ضغط زر التشغيل داخل الصفحة (غير موجود بالـ HTML مسبقاً) |
| مكان حقن الزر | مربع فارغ بين وصف الحلقة وصورة البوستر، بجانب زر "قائمة المشاهدة" (محدد بصورة سكرين شوت من المستخدم) |

**الخلاصة التقنية:** لا حاجة لاستخراج ترجمة منفصلة. المهمة الوحيدة هي التقاط رابط
الـ mp4 الموقّع وتمريره لـ PotPlayer بدل تشغيله بالمتصفح.

---

## 2. بنية الملفات

```
cinemana-potplayer/
├── manifest.json
├── background.js       (service worker — مراقبة الشبكة + تخزين الرابط)
├── content.js           (حقن الزر داخل صفحة الحلقة)
├── styles.css            (تنسيق الزر)
├── icons/
│   ├── icon16.png
│   ├── icon48.png
│   └── icon128.png
└── PLAN.md               (هذا الملف)
```

---

## 3. manifest.json

```json
{
  "manifest_version": 3,
  "name": "Cinemana → PotPlayer",
  "version": "1.0.0",
  "description": "فتح حلقات ومشاهد Cinemana مباشرة في PotPlayer",
  "permissions": ["webRequest", "storage", "tabs"],
  "host_permissions": [
    "*://*.shabakaty.com/*"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["*://cinemana.shabakaty.com/video/*"],
      "js": ["content.js"],
      "css": ["styles.css"],
      "run_at": "document_idle"
    }
  ],
  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  }
}
```

**ملاحظة صلاحيات:** `webRequest` (بدون `webRequestBlocking`) كافية للمراقبة فقط
(نحن لا نريد حجب/تعديل الطلب، فقط قراءة رابطه). `host_permissions` محصور على
نطاق `shabakaty.com` فقط — لا حاجة لـ `<all_urls>`.

---

## 4. background.js — منطق التقاط الرابط

### الخطوات
1. تسجيل مستمع `chrome.webRequest.onBeforeRequest` (أو `onResponseStarted`) مع
   فلتر URL: `*://cdn.shabakaty.com/vascin*_video.mp4*` و `*://cndw*.shabakaty.com/*_video.mp4*`
2. عند التقاط طلب مطابق: استخراج `tabId` من تفاصيل الطلب + الرابط الكامل
3. تخزين الرابط في `chrome.storage.session` بمفتاح مرتبط بـ `tabId`
   (مثال: `{"tab_123": {"url": "...", "capturedAt": 1755300000}}`)
4. عند إغلاق أو تنقل التاب (`chrome.tabs.onRemoved` / `onUpdated`) — تنظيف
   المدخل القديم من `storage.session` لمنع تراكم بيانات قديمة
5. الاستماع لرسائل من `content.js` عبر `chrome.runtime.onMessage`:
   - رسالة `GET_VIDEO_URL` مع `tabId` → يرجع آخر رابط محفوظ لهذا التاب أو `null`
   - رسالة `OPEN_POTPLAYER` مع الرابط → بناء `potplayer://{encodeURIComponent(url)}`
     وفتحه عبر `chrome.tabs.update({url: potplayerUrl})` على نفس التاب (المتصفح
     يسلّم البروتوكول لنظام التشغيل ليطلق PotPlayer، ثم التاب يبقى كما هو)

### نقاط انتباه
- لا نعترض **كل** طلبات mp4 — الفلتر محدد بنمط `vascin*_video.mp4` لتفادي
  التقاط ملفات وسيطة أو إعلانات
- الرابط بعد الـ 302 redirect هو الرابط الفعلي القابل للتشغيل — نلتقط الطلب
  الثاني (بعد التحويل) وليس الأول، أو نلتقط أي من الاثنين طالما فيه Signature صالح

---

## 5. content.js — حقن الزر

### الخطوات
1. عند تحميل صفحة `/video/*`، استخدام `MutationObserver` لمراقبة ظهور الحاوية
   التي تحتوي زر "قائمة المشاهدة" و"شاهد" (لأن المحتوى يُحمّل ديناميكياً بجافاسكربت)
2. بمجرد العثور على الحاوية: إنشاء عنصر `<button>` بنص "🎬 فتح في PotPlayer"
   وإدراجه في المربع الفارغ المحدد (بين الوصف وصورة البوستر، بنفس صف زر
   "قائمة المشاهدة")
3. عند الضغط على الزر:
   - استخراج `videoId` من رابط الصفحة الحالية (مثال: `3122801` من
     `/video/ar/3122801`)
   - إرسال رسالة `GET_VIDEO_URL` إلى `background.js`
   - إذا رجع رابط: إرسال رسالة `OPEN_POTPLAYER` بهذا الرابط
   - إذا لم يرجع رابط: إظهار tooltip/توست قصير "شغّل الفيديو أولاً ثم اضغط الزر"
4. مراقبة تغيّر الحلقة (تبديل حلقة بدون إعادة تحميل الصفحة بالكامل — الموقع
   SPA على الأغلب) عبر مراقبة تغيّر الـ URL (`history.pushState` hook أو
   `MutationObserver` إضافي على رابط الصفحة) لإعادة حقن الزر إذا لزم

---

## 6. styles.css — تنسيق الزر

- زر بنفس نمط أزرار الموقع (خلفية داكنة، حدود مستديرة، خط أبيض)
- أيقونة صغيرة (▶ أو 🎬) + نص "فتح في PotPlayer"
- حالة hover واضحة
- مطابقة تقريبية لحجم ومكان المربع الأصفر المحدد بالصورة المرجعية

---

## 7. تدفق الاستخدام الكامل (User Flow)

1. المستخدم يفتح صفحة حلقة على cinemana
2. الإضافة تحقن الزر تلقائياً بجانب "قائمة المشاهدة"
3. المستخدم يضغط ▶ تشغيل الفيديو بالمتصفح لثانية أو ثانيتين (هذا يولّد رابط
   الـ mp4 الموقّع ويُلتقط بالخلفية)
4. المستخدم يضغط زر "فتح في PotPlayer"
5. الإضافة تبني رابط `potplayer://` وتُطلقه
6. ويندوز يستقبل بروتوكول `potplayer://` ويشغّل PotPlayer بالفيديو مباشرة
7. (اختياري) الإضافة توقف تشغيل الفيديو بالمتصفح لتفادي التشغيل المزدوج

---

## 8. القيود والمخاطر المعروفة

| القيد | التفسير | الحل |
|---|---|---|
| الرابط مؤقت (Expires) | لو مرّ وقت طويل بين تشغيل الفيديو والضغط على الزر، الرابط ينتهي ويرفضه PotPlayer | تعليمات للمستخدم: اضغط الزر خلال ثوانٍ من بدء التشغيل |
| الترجمة مدمجة | لا يوجد ملف ترجمة قابل للفصل — الترجمة تظهر فقط لو كانت مدمجة أصلاً في نسخة الفيديو | لا حل تقني ممكن من جهة الإضافة؛ هذا قيد من مصدر الفيديو نفسه |
| تغيّر بنية الموقع مستقبلاً | لو Cinemana غيّر أسماء النطاقات (`cdn`/`cndw`) أو نمط الرابط، ينكسر الفلتر | يحتاج تحديث دوري لنمط الـ URL في manifest/background.js |
| بروتوكول `potplayer://` غير مسجّل | لو PotPlayer مو مثبت أو البروتوكول مو مسجّل بالريجستري، الفتح يفشل بصمت أو يطلب "اختر تطبيق" | التحقق يدوياً أن PotPlayer مثبت بأحدث نسخة تدعم `potplayer://` (تجربتك السابقة مع Stremio تؤكد إنه يدعمه) |

---

## 9. خطوات الاختبار

1. تحميل الإضافة كـ **Unpacked** عبر `brave://extensions` → تفعيل Developer Mode
   → Load unpacked → اختيار مجلد `cinemana-potplayer`
2. فتح صفحة حلقة (مثال: `REACHER` الموسم 4 الحلقة 1)
3. التأكد من ظهور الزر بالمكان الصحيح
4. تشغيل الفيديو لثانيتين، ثم ضغط الزر
5. التحقق أن PotPlayer يفتح ويشغّل الفيديو بنفس النقطة أو من البداية
6. اختبار حلقة ثانية بدون إعادة تحميل الصفحة (تبديل من قائمة الحلقات) للتأكد
   أن الزر يلتقط الرابط الصحيح للحلقة الجديدة

---

## 10. الترتيب الفعلي للتنفيذ

1. ✅ `manifest.json`
2. ✅ `background.js` (مراقبة الشبكة + تخزين + فتح البروتوكول)
3. ✅ `content.js` (حقن الزر + منطق الضغط)
4. ✅ `styles.css`
5. ✅ أيقونات بسيطة (placeholder)
6. اختبار يدوي من المستخدم وتعديل حسب النتائج الفعلية
